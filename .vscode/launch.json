{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb) Debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/CacheSystem",
      "args": [
        "-b",
        "input/adaptec1"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}